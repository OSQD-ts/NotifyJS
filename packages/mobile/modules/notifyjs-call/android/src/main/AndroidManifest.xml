<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    A full-screen intent is what makes an incoming call take over a locked
    screen instead of appearing as a banner. Android 14 requires the user to
    grant it for anything that is not a default calling app, which the module
    surfaces through canUseFullScreen()/openSettings().
  -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.VIBRATE" />

  <!--
    Keeps the process - and therefore the WebSocket - alive while the app is
    off screen. "specialUse" is the honest category: this is not media,
    location or a data sync, it is a long-lived connection the user asked for.
  -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <receiver
      android:name=".CallActionReceiver"
      android:exported="false" />

    <service
      android:name=".NotifyjsWatchService"
      android:exported="false"
      android:foregroundServiceType="specialUse"
      android:stopWithTask="false">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Maintains a connection to the user's own alerting hub so that
                       incoming alerts and calls arrive while the app is off screen." />
    </service>
  </application>
</manifest>
