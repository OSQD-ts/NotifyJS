<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    A full-screen intent is what makes an incoming call take over a locked
    screen instead of appearing as a banner. Android 14 requires the user to
    grant it for anything that is not a default calling app, which the module
    surfaces through canUseFullScreen()/openSettings().
  -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.VIBRATE" />

  <!--
    Keeps the process - and therefore the WebSocket - alive while the app is
    off screen. "specialUse" is the honest category: this is not media,
    location or a data sync, it is a long-lived connection the user asked for.
  -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <!--
    Watching for the network coming back, which is what a Wi-Fi to cellular
    handover looks like from here. Reading connectivity state only - no
    permission exists, or is wanted, for the traffic itself.
  -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application>
    <receiver
      android:name=".CallActionReceiver"
      android:exported="false" />

    <!--
      The Doze-proof heartbeat. Deliberately not exported: nothing outside the
      app has any business asking it to reconnect.
    -->
    <receiver
      android:name=".WatchAlarmReceiver"
      android:exported="false" />

    <!--
      Restores the ongoing notification when it is swiped away. Not exported:
      only this app's own delete intent may ask for it back.
    -->
    <receiver
      android:name=".WatchNotificationReceiver"
      android:exported="false" />

    <!--
      Notices that a restart has left the phone not alerting. Exported because
      BOOT_COMPLETED and MY_PACKAGE_REPLACED are system broadcasts and will not
      reach a receiver that is not; both are protected, so only the system can
      send them.
    -->
    <receiver
      android:name=".BootReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <service
      android:name=".NotifyjsWatchService"
      android:exported="false"
      android:foregroundServiceType="specialUse"
      android:stopWithTask="false">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Maintains a connection to the user's own alerting hub so that
                       incoming alerts and calls arrive while the app is off screen." />
    </service>
  </application>
</manifest>
